import { Post } from "@interface/Post";
import React from "react";

interface IProps {
  post: Post;
}

const ArticleCard: React.FC<IProps> = ({ post }) => {
  return <div></div>;
};

export default ArticleCard;
